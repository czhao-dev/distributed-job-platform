name: worker-api
namespace: default
path_prefix: /api
strategy: least_conn
# selector matches Node labels — nodes with role=worker receive traffic via this service
selector:
  role: worker
health_check:
  path: /healthz
  interval_seconds: 5
retry_policy:
  max_attempts: 3
  per_try_timeout_ms: 500
