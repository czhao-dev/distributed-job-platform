name: heartbeat-service
namespace: default
labels:
  app: heartbeat
  tier: service
type: service
command: "sleep"
args: ["120"]
replicas: 2
max_retries: 5
restart_policy: always
resources:
  cpu: 0.25
  memory_mb: 128
